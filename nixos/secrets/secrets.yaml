# Files must always have a string value
example-key: example-value
# Nesting the key results in creating directories.
# The deirectories will be owned by root:keys and have the permissions 0751:
myservice:
  my_subdir:
    my_secret: password1